import React from 'react';
import { Container } from 'react-bootstrap';

const Login = () => {
    return (
        <Container>
            <h2>This is Login</h2>
        </Container>
    );
};

export default Login;