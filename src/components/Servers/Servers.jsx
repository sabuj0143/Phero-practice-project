import React from 'react';
import { Container } from 'react-bootstrap';

const Servers = () => {
    return (
        <Container>
            <h2>Servers</h2>
        </Container>
    );
};

export default Servers;